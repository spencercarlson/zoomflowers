{
  "name": "zoomflowers.github.io",
  "version": "1.0.0",
  "description": "",
  "scripts": {
    "build": "python freeze.py && cp CNAME build/CNAME",
    "deploy": "npm run build && gh-pages -d build -b live && rm -rf build"
  },
  "dependencies": {
    "gh-pages": "^1.2.0"
  }
}